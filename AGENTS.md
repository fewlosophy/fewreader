## Audit Instructions

- Address the Python warnings or format/lint issues
- Look for typical application issues, missing type definitions, incorrect return types or parameter types
- Improve exception handling. Make sure we use fastAPI standard HTTP exceptions for expected errors instead of generic `Exception`.
