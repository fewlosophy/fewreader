# ReadLite Environment Configuration

# Directory containing .md and .txt books, articles, or notes (relative to project root or absolute path)
CONTENT_DIR=content

# Application title displayed in the navbar and PWA manifest
APP_TITLE=ReadLite

# Server port (used by Docker and local setups)
PORT=8000
